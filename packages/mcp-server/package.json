{
  "name": "jitre-mcp-server",
  "version": "0.3.0",
  "private": true,
  "description": "Model Context Protocol server that exposes Jitre projects, tasks, comments, time tracking and AI-assisted test cases as tools so Claude / Cursor / other MCP clients can read and act on a Jitre workspace.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "bin": {
    "jitre-mcp-server": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc --noEmit -p ./",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
