export declare enum CustomFieldType {
    TEXT = "text",
    NUMBER = "number",
    SELECT = "select",
    MULTI_SELECT = "multi_select",
    DATE = "date",
    BOOLEAN = "boolean",
    USER = "user"
}
export declare enum CustomFieldScope {
    WORKSPACE = "workspace",
    PROJECT = "project"
}
